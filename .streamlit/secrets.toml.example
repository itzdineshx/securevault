# Streamlit stores secrets in .streamlit/secrets.toml (do NOT commit real secrets)
# Copy this file to `.streamlit/secrets.toml` when deploying on Streamlit Cloud and update values.

[general]
backend_url = "http://localhost:8000"
